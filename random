float dx;
float dy;
float d;

void setup() {
  size(600,600);
  background(51);
}

void draw() {
  dx = mouseX;
  dy = mouseY;
  d = sqrt(dx*dx + dy*dy);
  stroke(random(255),random(255),random(255));
  strokeWeight(random(2,15));
  point(dx,dy);
  println(d);
}
